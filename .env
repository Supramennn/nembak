CI_ENVIRONMENT = production

# app.baseURL akan di-set otomatis oleh Vercel via environment variable
# Set ini di Vercel Dashboard → Settings → Environment Variables
# app.baseURL = 'https://your-project.vercel.app/'

# Matikan debug toolbar di production
# app.debugToolbar = false
